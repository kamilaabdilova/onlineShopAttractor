-- Create table
CREATE TABLE user (
                      id INT PRIMARY KEY,
                      name VARCHAR(50),
                      age INT
);

-- Insert test data
INSERT INTO user (id, name, age) VALUES (1, 'John', 30);
INSERT INTO user (id, name, age) VALUES (2, 'Mary', 25);
